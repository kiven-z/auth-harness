name: negative-role-rename
description: 仅改角色名称不应触发授权失效 outbox
steps:
  - update_role_meta:
      role_id: 9000200004
      roleName: Harness 共享角色（重命名测试）
  - assert_no_outbox:
      source_biz_id_contains: "R_SHARED"
      grace_sec: 2
